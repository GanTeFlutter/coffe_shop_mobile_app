import 'package:flutter/material.dart';

class ShowMBSheet extends StatelessWidget {
  const ShowMBSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}